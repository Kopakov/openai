import { Typography } from '@mui/material';

const Header = () => {
  return (
    <Typography
      variant='h4'
      textAlign='center'
      sx={{
        mb: 3,
        fontWeight: 700,
      }}
    >
      💻 Ask AI 🧠
    </Typography>
  );
};

export default Header;
